language: python

python:
  - "3.6"

env:
  - FLASK_APP=main.py

install:
  - pip install -r zeeguu_dashboard/requirements.txt

script:
  - export PYTHONPATH=$PYTHONPATH:$(pwd)
  - pushd zeeguu_dashboard/tests/
  - python test_foo.py
  - popd