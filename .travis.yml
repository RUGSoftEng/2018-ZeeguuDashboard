language: python

python:
  - "3.6"

env:
  - FLASK_APP=main.py

install:
  - pip install -r requirements.txt

script:
  - cd zeeguu_dashboard
  - py.test --cov=./ tests/

after_success:
  - coveralls